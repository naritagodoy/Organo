import './Banner.css'

export const Banner = () => {
    // JSX como o react trabalha e transforma em elementos no DOM
    return (
        <header className='banner'>
        <img src="/imagens/banner.png" alt="banner principal da página do organo"/>
        </header>
    )
}

